# Write a program which accepts a sequence of comma-separated numbers from console and generate a list.

numbers=input("write some coma-separated numbers\n") # taking list from user
list = numbers.split(",")                            # creating a list with coma seperator 
print("genrated list is",list)                       # printing the list
